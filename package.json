{
  "name": "mosh",
  "version": "1.0.0",
  "description": "CSS compiler for the Mothership system",
  "scripts": {
    "build": "gulp css",
    "build:watch": "gulp",
    "compile": "gulp css",
    "watch": "gulp",
    "gulp": "gulp",
    "test:quench": "npm run build && npm run build --prefix quench && npm run tests:ci --prefix quench"
  },
  "browserslist": [
    "last 3 versions"
  ],
  "author": "Martin Jencka",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "gulp": "^5.0.1",
    "gulp-autoprefixer": "^8.0.0",
    "gulp-rename": "^2.1.0",
    "gulp-sass": "^6.0.1",
    "gulp-sourcemaps": "^3.0.0",
    "sass": "^1.93.2"
  }
}
